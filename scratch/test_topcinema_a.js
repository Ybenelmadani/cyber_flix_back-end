const axios = require('axios'); const cheerio = require('cheerio'); axios.get('https://topcinemaa.top/search/House%20of%20the%20Dragon', {headers: {'User-Agent': 'Mozilla/5.0'}}).then(r => { const $ = cheerio.load(r.data); const links = []; $('a').each((i, el) => links.push($(el).attr('href'))); console.log('Found:', links.filter(l => l && l.includes('house-of-the-dragon'))); }).catch(console.error);
